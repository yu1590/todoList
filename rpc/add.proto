syntax = "proto3";

package add;

option go_package = "./add";

message addReq {
    int64 accountID = 1;
    repeated int64 months = 2;
}

message addResp {
    bool ok = 1;
}

service adder {
    rpc getTodoList(addReq) returns(addResp);
}